# TodoList API REST - Carnet 24002799

## Requisitos Previos
- Node.js v18+ (para ejecución directa)
- Docker Desktop (para ejecución mediante contenedores)

## Ejecución Local (Sin Docker)
1. Instalar dependencias:
   ```bash
   npm install